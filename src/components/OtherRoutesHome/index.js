import React from "react";

import "./style.sass";
const OtherRoutesHome = ({ heading }) => {
  return (
    <section className="other-home-section">
      <div className="container">
        <h1>{heading}</h1>
      </div>
    </section>
  );
};

export default OtherRoutesHome;
